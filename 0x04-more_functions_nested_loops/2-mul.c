
#include "main.h"

/**
 * mul - multiplies two integers.
 * @a: the multiplicand
 * @b: the multiplier
 * Return: 0 if exited properly, non-zero otherwise
 */
int mul(int a, int b)
{
	return (a * b);
}
