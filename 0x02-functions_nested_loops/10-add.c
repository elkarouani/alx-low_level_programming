#include "main.h"

/**
 * add - adds two integers and returns the result.
 * @a: an addend for addition
 * @b: an addend for addition
 * Return: addition result
 */
int add(int a, int b)
{
	return (a + b);
}
